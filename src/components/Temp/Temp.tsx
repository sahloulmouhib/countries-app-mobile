import React from 'react';
import { View, Text } from 'react-native';

type Props = {};

const Temp = (props: Props) => {
  return (
    <View>
      <Text>Temp</Text>
    </View>
  );
};

export default Temp;
