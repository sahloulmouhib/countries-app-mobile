export type LoaderState = {
  isVisible: boolean;
  isGlobal: boolean;
  whiteBackGround?: boolean;
};

export type LoaderParams = {
  isVisible?: boolean;
  isGlobal?: boolean;
  whiteBackGround?: boolean;
};
