import { StyleSheet } from 'react-native';

import { colors } from '_utils/theme/colors';
import { FONT_FAMILY } from '_utils/theme/fonts';

export default StyleSheet.create({
  text: {
    fontFamily: FONT_FAMILY,
    color: colors.BLACK,
    fontSize: 20,
  },
});
