import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  padding: {
    padding: 16,
  },

  emptyList: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
