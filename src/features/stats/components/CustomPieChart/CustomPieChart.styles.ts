import { StyleSheet } from 'react-native';

import { colors } from '_utils/theme/colors';

export default StyleSheet.create({
  pieChart: {
    backgroundColor: colors.GREY_LIGHT + '80',
    borderRadius: 10,
    height: 250,
    justifyContent: 'center',
  },
});
