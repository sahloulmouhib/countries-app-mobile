import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 16,
    width: '100%',
    justifyContent: 'space-between',
    flexDirection: 'row',
  },
});
