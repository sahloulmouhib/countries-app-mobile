export enum QuizType {
  Flag = 'flag',
  Capital = 'capital',
  HigherOrLowerPopulation = 'higherOrLowerPopulation',
  Memory = 'memory',
}

export enum AnswerType {
  Correct = 'correct',
  Incorrect = 'incorrect',
  Default = 'default',
}
