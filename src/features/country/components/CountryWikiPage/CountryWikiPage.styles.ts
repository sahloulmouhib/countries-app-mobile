import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  closeButton: {
    padding: 16,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
